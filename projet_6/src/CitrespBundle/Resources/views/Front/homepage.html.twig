{# src/CitrespBundle/Resources/views/Front/homepage.html.twig #}

{% extends '@Citresp/layout.html.twig' %}

{% block stylesheets %}
  {{ parent() }}
  <link rel="stylesheet" href="{{ asset('theme/css/sign-in.css') }}">
{% endblock %}

{% block body %}
  <div class="container container-intro">
    <div class="row justify-content-md-center">
      <div class="co col-md-10 col-md-8container-intro text-center">
        <h5>
          {# <strong> #}
            En créant un compte vous pourrez signaler un problème de voirie, d’éclairage public, de déchets… Et en suivre l’évolution.
            Vous aurez aussi une visibilité sur l’ensembles des signalements de votre ville que vous pourrez commenter si nécessaire.
          {# </strong> #}
        </h5>
      </div>
    </div>
      
      

    {% for msg in app.session.flashBag.get('errorSelectedBaseCities') %}
      <div class="flashBag">
        <p class="alert alert-danger" role="alert">{{ msg }}</p>
      </div>
    {% endfor %}

    {% for msg in app.session.flashBag.get('SuccessRemoveUser') %}
      <div class="flashBag">
        <p class="alert alert-success" role="alert">{{ msg }}</p>
      </div>
    {% endfor %}

    {% for msg in app.session.flashBag.get('errorCityAccess') %}
    <div class="flashBag">
      <p class="alert alert-danger" role="alert">{{ msg }}</p>
    </div>
    {% endfor %}

    <div class="contaner-sign-in text-center">
      {% embed "@Citresp/FormViews/citySelectForm.html.twig" %}{% endembed %}
    </div>
  </div>

  <div class="container container-pading-t-b bg-primary text-center home-create">
    <h3>Créez un compte pour votre ville</h3>
    <p>Si aucun compte n'existe pour votre ville, vous pouvez en créer un.
      <br/>
      <strong>Vous serez alors l'administrateur principal de ce compte.</strong>
    </p>

    <div class="contaner-sign-in text-center">
      {% embed "@Citresp/FormViews/baseCitiesSearchForm.html.twig" %}{% endembed %}
    </div>

  </div>

{% endblock %}
